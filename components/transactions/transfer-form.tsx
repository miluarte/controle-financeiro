'use client'

import { Card, CardContent } from '@/components/ui/card'

export function TransferForm() {
  return (
    <Card>
      <CardContent className="p-4">
        <p className="text-sm text-muted-foreground">Formulário de transferência — em breve</p>
      </CardContent>
    </Card>
  )
}
